<!DOCTYPE html>
html(lang="en")
head
  meta(charset="UTF-8")
  script(type="text/javascript" src="http://code.jquery.com/jquery-1.10.2.min.js")
  link(href="/css/PixelPainter.css" rel="stylesheet" media="screen")
  title Your Painting
body
  h1 Your Painting
  
  div(id="pixelPainter")

    //- using '-' before code in jade allows you to use regular javascript
    //- colorGrid
    - var colorId = 0;
    #colorGrid.container
      - for (var column = 0; column < x; column++)
          div.row
            - for (var row = 0; row < y; row++)
              - colorId++
                div(id="colorCell#{colorId}" class="cell")

    - var paintId = 0;
    #paintGrid.container
      - for (var column = 0; column < a; column++)
          div.row
            - for (var row = 0; row < b; row++)
              - paintId++
                div(id="paintCell#{paintId}" class="cell" style="background-color: #{result['paintCell' + paintId]}")
                  //- result is an object now so we can use [] notation to place background-color in respect to each paintCell it was saved to previously
    - console.log(result);
    h2 By: 
    p #{author}

    h2 Description:
    p #{description}

    div

      button(id="eraseButton" class="button" name="button") Erase

      button(id="clearButton" class="button" name="button") Clear

      button(id="saveButton" class="button" name="button") Save

  script(src="/js/PixelPainter.js")